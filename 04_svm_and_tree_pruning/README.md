# SVM and Decision-Tree Pruning

Compares a scaled RBF-kernel SVM with a decision tree. It searches cost-complexity-pruning (`ccp_alpha`) values to find a tree that generalizes better than an unconstrained tree.

Run: `python 04_svm_and_tree_pruning/run_svm_pruning.py`
